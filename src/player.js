class Player {
  static initialize() {
    this.keyStatus = {
      right: false,
      left: false,
      down: false,
      rotationR: false,
      rotationL: false
    };
    
    document.addEventListener('keydown', (e) => {
      switch ((e.keyCode)) {
        case :
          // code
          break;
        
      }
    })
  }
}